#include "holberton.h"

/**
 * print_last_digit - returns last value of a digit.
 * @a: The value to be evaluated.
 *
 * Return: Last digit of evaluated value.
 */

int print_last_digit (int a)
{
	return(a % 10);
}
