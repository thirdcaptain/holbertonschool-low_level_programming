#include "holberton.h"

void _puts_recursion(char *s)
{
/*	if (*s)
	{
		_puts_recursion(s+1);
		_putchar(*s);
	}
	if (*s == '\0')
		_putchar('\n');
*/
	if (*s == '\0')
	{
		_putchar('\n');
		return;
	}
	_putchar(*s);
	_puts_recursion(s + 1);
}
